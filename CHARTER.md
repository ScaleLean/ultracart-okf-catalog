# project_ultracart_okf_catalog

**Owner:** data_warehouse
**Status:** active - public merchant-neutral ScaleLean GitHub repo published; local read-only OKF companion prototype still passes; live Google lab re-verification blocked by non-interactive gcloud auth refresh failure
**Primary tool:** Codex
**Current agent:** Codex
**Asana:** -

## Outcome(s) (done = )
The UltraCart BigQuery warehouse OKF toolkit is a merchant-neutral public repository that contains the standard UltraCart warehouse OKF catalog and can generate metadata-only OKF v0.1 bundles for any authorized `ultracart-dw-*` merchant project.

Done means:
- The public repository contains reusable scripts, validation, viewer generation, and a template config.
- The public repository contains the merchant-neutral standard UltraCart warehouse OKF bundle.
- The public repository does not include merchant-specific generated OKF bundles or reports.
- The generator requires an explicit source BigQuery project instead of defaulting to one merchant.
- The active project card points to the public ScaleLean organization repo and states the next useful expansion.

## Next action
Chris or an interactive agent should refresh gcloud auth (`gcloud auth login` or select a valid account), then re-run `./prototypes/google_bigquery_mcp_okf/verify_operational.sh` and compare it with `evidence/okf_companion_acceptance_2026-07-02.json`. Do not test against a merchant warehouse without explicit approval.

## Threads
- Merchant-neutral public repo — done — removed merchant-specific labels and generated merchant bundle output from the public branch.
- Standard warehouse catalog — done — generated the tracked `okf/ultracart_warehouse/` OKF bundle covering 8 standard datasets, 244 standard objects, 112 canonical table definitions, and full live schema paths for all standard objects.
- Single-merchant bundle generation — supported locally — use `configs/ultracart_merchant_template.yml` with an authorized source project.
- Downstream view augmentation — supported locally — pass explicit downstream and billing projects.
- Multi-merchant expansion — parked — requires a deliberate access/cost/sensitivity decision before inspecting multiple merchant projects.
- BigQuery MCP proposal — drafted — reframed around Google's managed BigQuery MCP endpoint as the live query/metadata server, with UltraCart owning the OKF semantic companion layer and lightweight BigQuery metadata breadcrumbs.
- Minimal OKF companion spec/prototype — prototype built — `prototypes/google_bigquery_mcp_okf/OKF_COMPANION_MINIMAL_SPEC_2026-07-02.md` defines v0.1 resources, structured tool contracts, agent workflow, lab acceptance tests, and non-goals; `prototypes/google_bigquery_mcp_okf/ultracart_okf_companion.py` implements the tiny local read-only companion over public OKF files.
- Google BigQuery MCP lab — operational — synthetic setup/cleanup/verification files are in `prototypes/google_bigquery_mcp_okf/`; dataset `okf_mcp_lab` and table `uc_orders` exist in clean project `ultracart-mcp-development`; operational evidence is under `prototypes/google_bigquery_mcp_okf/evidence/operational_2026-07-01/`.
- Local BigQuery MCP install — complete — Codex and Claude Code both point at local stdio server `/Users/openclaw-atlas/.local/bin/google-bigquery-mcp`, which forwards to Google's hosted BigQuery MCP endpoint with fresh `gcloud` credentials. Codex passed an in-thread MCP test; Claude Code MCP health reports connected, but `claude -p` agent testing requires Claude CLI login.
- Shared skill layer — complete — `skills/ultracart-bigquery/SKILL.md` now captures the operating rules for using Google BigQuery MCP with the UltraCart OKF/native warehouse route, while the older `skills/ultracart-data-warehouse/` skill is routed toward CEF/downstream analytics.

## Time estimate
15-30 min to refresh gcloud auth interactively and re-run the synthetic lab verification; local companion acceptance already passes.

## Context
This project implements an Open Knowledge Format layer for UltraCart BigQuery warehouse projects. It is a reusable catalog/read layer, not a competing source of truth for project status or dbt logic.

Guardrails:
- Do not query or store raw customer rows, raw emails, addresses, payment fields, session payloads, conversation bodies, or sampled rows.
- Generate concept docs from BigQuery metadata only.
- Treat generated bundles as merchant-specific local artifacts unless reviewed for public sharing.

## Log
- 2026-07-01 - Codex - Converted the public repository into a merchant-neutral UltraCart OKF toolkit: removed generated merchant-specific bundle output and reports, replaced the config with an explicit-project template, removed hardcoded merchant defaults from scripts, and prepared the public `main` branch for history rewrite.
- 2026-07-01 - Codex - Added the initial merchant-neutral UltraCart warehouse OKF catalog under `okf/ultracart_warehouse/`, including a static viewer, validation, and a self-test report. Verified no forbidden merchant-specific references in the repo tree or git object store.
- 2026-07-01 - Codex - Mirrored the standard OKF bundle directories to the repository root so GitHub links such as `tables/ultracart_dw_medium/uc_affiliate_clicks.md` resolve directly while preserving `okf/ultracart_warehouse/` for validation and regeneration workflows.
- 2026-07-01 - Codex - Corrected the standard scope by excluding the merchant-specific custom work dataset from the published standard catalog. Regenerated the root and nested bundles at 8 datasets, 244 standard objects, 112 canonical table definitions, and 48,633 flattened schema paths.
- 2026-07-01 - Codex - Updated `PROPOSAL_ULTRACART_BIGQUERY_MCP.md` after reviewing Google's BigQuery MCP docs: Google's managed `https://bigquery.googleapis.com/mcp` endpoint should be the live BigQuery MCP, while UltraCart's V2 opportunity is an OKF companion/profile that supplies warehouse semantics, guardrails, and optional preflight helpers.
- 2026-07-01 - Codex - Continued Google BigQuery MCP discovery with a live unauthenticated `tools/list` check. Confirmed the endpoint exposes six tools, both SQL tools support `dryRun: true`, `get_table_info` exposes table descriptions/labels/resource tags/schema, and the current BigQuery endpoint does not expose OKF-like resources/prompts through unauthenticated `resources/list` or `prompts/list`; updated the proposal to recommend BigQuery metadata breadcrumbs plus an OKF companion MCP.
- 2026-07-01 - Codex - Added a local Google BigQuery MCP + UltraCart OKF prototype packet under `prototypes/google_bigquery_mcp_okf/`, including synthetic lab DDL, setup/cleanup wrappers, Google MCP dry-run and execute payloads, an expected OKF companion response, and an agent test prompt. No GCP resources were created or modified.
- 2026-07-01 - Codex - Created the synthetic BigQuery lab in clean project `ultracart-mcp-development` and verified Google MCP metadata discovery and dry-run/execution against `okf_mcp_lab.uc_orders`. Also identified and corrected a scope error: CEF downstream dbt/core/reporting tables are not valid proof surfaces for the native UltraCart OKF behavior; future CEF validation should use only the native UltraCart warehouse if access is explicitly available.
- 2026-07-01 - Codex - Re-ran the operational proof with `verify_operational.sh`: Google BigQuery MCP can discover `okf_mcp_lab`, read OKF labels/descriptions on `uc_orders`, dry-run the aggregate query with `execute_sql_readonly` and `dryRun: true`, and execute the bounded aggregate against synthetic rows only. Evidence saved under `prototypes/google_bigquery_mcp_okf/evidence/operational_2026-07-01/`.
- 2026-07-02 - Codex - Installed local `google-bigquery` MCP access for both Codex and Claude Code using `/Users/openclaw-atlas/.local/bin/google-bigquery-mcp`, a gcloud-authenticated stdio bridge to Google's hosted BigQuery MCP endpoint. Direct bridge tests passed, Codex exec thread `019f2051-2a01-7801-acc6-db388172d525` verified `okf_mcp_lab.uc_orders` visibility and dry-run status, and Claude Code MCP health reports connected; Claude agent-thread testing is blocked until `claude` CLI auth is restored.
- 2026-07-02 - Codex - Added shared workspace skill `skills/ultracart-bigquery/SKILL.md` titled "Ultracart BigQuery" to layer project boundaries, safe MCP query sequence, OKF interpretation, and CEF/downstream scope guardrails on top of the installed `google-bigquery` MCP server. Updated the local Codex `ultracart-bigquery` skill to delegate to the shared workspace skill, and added a routing note to `skills/ultracart-data-warehouse/SKILL.md` so CEF-specific downstream work stays separate from native UltraCart OKF proof.
- 2026-07-01 - Hermes - Advanced the next local/safe step: re-ran `prototypes/google_bigquery_mcp_okf/verify_operational.sh` successfully against synthetic project `ultracart-mcp-development.okf_mcp_lab.uc_orders` (dataset/table visible, OKF labels/descriptions present, `execute_sql_readonly` dry-run succeeded, bounded aggregate returned 4 rows with 0 bytes billed) and drafted `prototypes/google_bigquery_mcp_okf/OKF_COMPANION_MINIMAL_SPEC_2026-07-02.md`. No merchant/client data or external writes were used beyond the existing synthetic lab verification script.
- 2026-07-02 - Hermes - Built `prototypes/google_bigquery_mcp_okf/ultracart_okf_companion.py`, a local read-only OKF companion prototype that lists/serves merchant-neutral OKF resources, interprets BigQuery OKF breadcrumbs, returns `uc_orders` grain/currency/sensitivity context, and preflights SQL into a recommended Google `execute_sql_readonly` dry-run payload. Verified with `python3 ... list-resources`, `python3 ... acceptance`, `python3 -m py_compile`, and an assertion script; local acceptance evidence saved to `prototypes/google_bigquery_mcp_okf/evidence/okf_companion_acceptance_2026-07-02.json`. Attempted live lab re-verification with `./prototypes/google_bigquery_mcp_okf/verify_operational.sh`, but it is blocked by expired non-interactive gcloud auth (`gcloud.auth.print-access-token`: reauthentication failed). No merchant warehouse or customer data was touched.
- 2026-07-04 - Hermes - Evening autonomous review picked the OKF catalog as the safest small read-only verification target. Re-ran `./prototypes/google_bigquery_mcp_okf/verify_operational.sh`; it still exits 1 because `gcloud.auth.print-access-token` cannot refresh credentials non-interactively (`Reauthentication failed. cannot prompt during non-interactive execution.`). Re-ran local companion acceptance and py_compile successfully with `python3 prototypes/google_bigquery_mcp_okf/ultracart_okf_companion.py acceptance && python3 -m py_compile ...`; output confirmed high-confidence UltraCart breadcrumbs, `uc_orders` OKF context, currency/grain/PII warnings, and a recommended `execute_sql_readonly` dry-run payload. No merchant warehouse or customer data was touched.
- 2026-07-06 - Claude Code (Fable) - Built the **UltraCart Store Ontology** under `ontology/` — OKF layer 2, merchant-neutral, owner-delegated overnight ("100/100 base ontology any UltraCart merchant can build upon"). Contents: SPEC.md (file formats; compiled-SQL-only rule), identity spine (email_hash base64/hex bridge, oid families, string-code joins, merchant_id scoping), 28 object files across 12 domains (from all 112 canonical tables; coverage ledger total), 7 parameterized named definitions (revenue_order per UltraCart's own convention, customer, active/paused/lapsed_subscriber, repeat_customer, marketing_reachable), 17 governed action files + full 371-op mutation catalog from the live OpenAPI spec, compiler (config -> per-merchant DDL + manifest), offline validator (field-path validation against OKF layer 1 — drift detection without BigQuery auth), index generator (link graph: 79 links). Inputs: subagent mining of all layer-1 table docs + UltraCart's OpenAPI/Confluence/skill-repo docs. Validation: offline PASS (0 failures); CEF instance compiled and all 35 views dry-run validated live via read-only BigQuery MCP (evidence in VALIDATION.md, merchant-side detail in CE workspace). Committed locally; **public push left as an owner decision (D20)**. CE's 03_knowledge/ontology is now overlay-only on this base.
